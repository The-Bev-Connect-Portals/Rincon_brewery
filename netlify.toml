[build]
  publish = "."
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=3600"

# brand.config.js changes per deploy — don't let it go stale in a CDN.
[[headers]]
  for = "/brand.config.js"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Repo moved to The-Bev-Connect-Portals org, 2026-08-18.
# Netlify GitHub App reinstalled on the org; this comment exists to
# confirm the webhook survived the transfer. Safe to delete.
